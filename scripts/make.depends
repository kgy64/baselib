#
#

# Use only the actually existing dependency files:
MY_DEPENDS       =  $(shell find $(SRCDIR) -name "*.depend")

.PHONY: all
all: _depends

include $(SCRIPTDIR)/makedirect

.PHONY: _depends
_depends: $(DEPENDS)

ifneq ($(MY_DEPENDS), "")
include $(MY_DEPENDS)
endif

