#
#

export PROJECT_ROOT     ?=  $(PWD)
export BINDIR           ?=  $(PROJECT_ROOT)/bin
export SCRIPTDIR        ?=  $(PROJECT_ROOT)/scripts

export PUBLIC_INCLUDES  ?=  $(shell find -L $(PROJECT_ROOT) -name include -exec $(SCRIPTDIR)/echo_public_include_dir "{}" \;)

